describe("Searching user service:", () => {
  it("", () => {
    expect(2 + 2).toBe(4);
  });

  it("", () => {
    expect(2 + 2).toBe(4);
  });

  it("", () => {
    expect(2 + 2).toBe(4);
  });
});
